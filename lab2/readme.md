

### **Норма вектора**

**Норма вектора** — это неотрицательное действительное число, которое ставится в соответствие каждому вектору $x \in \mathbb{R}^{m}$ и обозначается $\|x\|$. Оно характеризует «длину» или «величину» вектора.

Норма должна удовлетворять трём основным свойствам (аксиомам) для любых векторов $x, y \in \mathbb{R}^{m}$ и любого скаляра $\alpha \in \mathbb{R}$:

1.  **Неотрицательность и определённость:**
    $\|x\| \ge 0$, причём $\|x\| = 0$ тогда и только тогда, когда $x = 0$.

2.  **Однородность:**
    $\|\alpha x\| = |\alpha| \cdot \|x\|$.

3.  **Неравенство треугольника:**
    $\|x + y\| \le \|x\| + \|y\|$.

### **Наиболее распространенные нормы в $\mathbb{R}^{m}$**

В пространстве $\mathbb{R}^{m}$ наиболее часто используются следующие нормы:

*   **Норма $L_1$** (или манхэттенская норма):
    $\|x\|_1 = \sum_{i=1}^{m} |x_i|$

*   **Норма $L_2$** (или евклидова норма):
    $\|x\|_2 = \sqrt{\sum_{i=1}^{m} x_i^2}$

*   **Норма $L_\infty$** (или равномерная норма, норма Чебышёва):
    $\|x\|_\infty = \max_{1 \le i \le m} |x_i|$

---




### **2. Определение нормы матрицы, подчинённой норме вектора. Формулы вычисления. Верхняя и нижняя треугольные матрицы**

Норма матрицы, **подчинённая векторной норме** $\|\cdot\|$, определяется как:

$$
\|A\| = \sup_{x \neq 0} \frac{\|Ax\|}{\|x\|} = \max_{\|x\| = 1} \|Ax\|
$$

Для трёх основных векторных норм получаем соответствующие подчинённые матричные нормы:

*   **Норма, подчинённая $\|\cdot\|_1$ (столбцовая норма):**
    $$
    \|A\|_1 = \max_{1 \le j \le n} \sum_{i=1}^n |a_{ij}|
    $$
    *(максимальная сумма модулей элементов по **столбцам*)*

*   **Норма, подчинённая $\|\cdot\|_\infty$ (строчная норма):**
    $$
    \|A\|_\infty = \max_{1 \le i \le n} \sum_{j=1}^n |a_{ij}|
    $$
    *(максимальная сумма модулей элементов по **строкам*)*

*   **Норма, подчинённая $\|\cdot\|_2$ (спектральная норма):**
    $$
    \|A\|_2 = \sqrt{\lambda_{\max}(A^T A)}
    $$
    где $\lambda_{\max}$ — наибольшее собственное значение матрицы $A^T A$.

### **Треугольные матрицы**


#### 🔺 Верхняя треугольная матрица

Все элементы **ниже** главной диагонали равны нулю:
$a_{ij} = 0$ при $i > j$.

**Пример:**

$$
U = \begin{pmatrix}
2 & -1 & 3 \\
0 & 4 & 5 \\
0 & 0 & 6
\end{pmatrix}
$$

Здесь $u_{ij} = 0$ при $i > j$.

---

#### 🔻 Нижняя треугольная матрица

Все элементы **выше** главной диагонали равны нулю:
$a_{ij} = 0$ при $i < j$.

**Пример:**

$$
L = \begin{pmatrix}
1 & 0 & 0 \\
7 & -2 & 0 \\
3 & 8 & 5
\end{pmatrix}
$$

Здесь $l_{ij} = 0$ при $i < j$.


---

### **3. Сходимость по норме**

Последовательность векторов $\{x_n\}$ является сходящейся по норме к вектору $x$, если норма разности стремится к нулю при $n \rightarrow \infty$.

**Математически:**

$$
\lim_{n \rightarrow \infty} \|x_n - x\| = 0
$$



### 4. Метод простой итерации

#### Итерационная формула
Исходную систему уравнений  
$A x = b$  
приводят к эквивалентному виду:  
$x = B x + c$

Итерационный процесс строится по формуле:  
$x^{(k+1)} = B x^{(k)} + c, \quad k = 0, 1, 2, \dots$

**Способ построения B и c:**
- Выделяют диагональную часть матрицы A:  
  $D = \operatorname{diag}(a_{11}, a_{22}, \dots, a_{nn})$
- Тогда:  
  $B = I - D^{-1} A, \quad c = D^{-1} b$

#### Перестановка строк
Матрица A не всегда подходит для итерационного метода «как есть». Чтобы обеспечить сходимость, часто **меняют местами уравнения** (переставляют строки A и соответствующие компоненты вектора b) так, чтобы **на главной диагонали оказались доминирующие по модулю элементы**.

**Цель перестановки** — добиться **диагонального преобладания**:  
$|a_{ii}| > \sum_{j \ne i} |a_{ij}| \quad \text{для всех } i$

Если это удалось — сходимость метода простой итерации **гарантирована**.

#### Условие сходимости
Метод сходится при любом начальном приближении, если:  
$\|B\| < 1$  
в какой-либо матричной норме (например, ℓ₁ или ℓ∞).

**Достаточное условие:** диагональное преобладание матрицы A после перестановки строк.

#### Скорость сходимости
- Сходимость — **линейная**
- Чем меньше норма $\|B\|$, тем быстрее убывает ошибка
- Если $\|B\| \approx 0.3$ — метод сходится быстро
- Если $\|B\| \approx 0.9$ — метод сходится очень медленно

#### Критерий окончания
Если известно, что $\|B\| = q < 1$, то итерации останавливают, когда:  
$\|x^{(k)} - x^{(k-1)}\| < \frac{1 - q}{q} \cdot \varepsilon$  
где ε — требуемая точность.

**Более надёжный критерий** — **невязка**:  
$\|A x^{(k)} - b\| < \varepsilon$

---

### 5. Метод Зейделя

#### Итерационная формула
Метод Зейделя — улучшенная версия простой итерации. При вычислении i-й компоненты нового приближения **уже используются обновлённые значения** $x_1^{(k+1)}, \dots, x_{i-1}^{(k+1)}$.

**Покомпонентная формула:**  
$x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j=1}^{i-1} a_{ij} x_j^{(k+1)} - \sum_{j=i+1}^{n} a_{ij} x_j^{(k)} \right)$

**Матричная форма:**  
$x^{(k+1)} = (D - L)^{-1} U x^{(k)} + (D - L)^{-1} b$  
где:
- L — строго нижняя треугольная часть A
- U — строго верхняя треугольная часть A

#### Перестановка строк
Как и в методе простой итерации, **перестановка строк** может быть необходима для обеспечения диагонального преобладания. Без этого метод может **не сходиться**.

#### Условие сходимости
Метод Зейделя сходится, если выполнено хотя бы одно из условий:
- Матрица A имеет **строгое диагональное преобладание**
- Матрица A **симметричная и положительно определённая**

**Важно:** даже если метод простой итерации сходится, метод Зейделя **сходится не медленнее**, а чаще — **значительно быстрее**.

#### Скорость сходимости
- Сходимость — **линейная**
- В **2–5 раз быстрее**, чем у простой итерации
- Особенно эффективен при слабой связи между переменными
- Скорость зависит от спектрального радиуса матрицы перехода  
  $B_{\text{З}} = (D - L)^{-1} U$

#### Критерий окончания
Используют те же критерии, что и для простой итерации:
- Разность между итерациями: $\|x^{(k)} - x^{(k-1)}\| < \delta$
- **Невязка**: $\|A x^{(k)} - b\| < \varepsilon$

**Предпочтительнее использовать невязку**, так как не требует знания нормы матрицы B.

---

## Ключевая роль перестановки строк

- Матрица A **не обязана** изначально подходить для итерационных методов
- **Перестановка строк** — допустимая операция (не меняет решение), позволяющая:
  - Разместить крупные по модулю элементы на диагонали
  - Добиться диагонального преобладания
  - Обеспечить $\|B\| < 1$ и **гарантированную сходимость**
- Без такой перестановки итерационные методы могут **расходиться**, даже если решение системы существует
