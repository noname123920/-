

### **Норма вектора**

**Норма вектора** — это неотрицательное действительное число, которое ставится в соответствие каждому вектору $x \in \mathbb{R}^{m}$ и обозначается $\|x\|$. Оно характеризует «длину» или «величину» вектора.

Норма должна удовлетворять трём основным свойствам (аксиомам) для любых векторов $x, y \in \mathbb{R}^{m}$ и любого скаляра $\alpha \in \mathbb{R}$:

1.  **Неотрицательность и определённость:**
    $\|x\| \ge 0$, причём $\|x\| = 0$ тогда и только тогда, когда $x = 0$.

2.  **Однородность:**
    $\|\alpha x\| = |\alpha| \cdot \|x\|$.

3.  **Неравенство треугольника:**
    $\|x + y\| \le \|x\| + \|y\|$.

### **Наиболее распространенные нормы в $\mathbb{R}^{m}$**

В пространстве $\mathbb{R}^{m}$ наиболее часто используются следующие нормы:

*   **Норма $L_1$** (или манхэттенская норма):
    $\|x\|_1 = \sum_{i=1}^{m} |x_i|$

*   **Норма $L_2$** (или евклидова норма):
    $\|x\|_2 = \sqrt{\sum_{i=1}^{m} x_i^2}$

*   **Норма $L_\infty$** (или равномерная норма, норма Чебышёва):
    $\|x\|_\infty = \max_{1 \le i \le m} |x_i|$

---




### **2. Определение нормы матрицы, подчинённой норме вектора. Формулы вычисления. Верхняя и нижняя треугольные матрицы**

Норма матрицы, **подчинённая векторной норме** $\|\cdot\|$, определяется как:

$$
\|A\| = \sup_{x \neq 0} \frac{\|Ax\|}{\|x\|} = \max_{\|x\| = 1} \|Ax\|
$$

Для трёх основных векторных норм получаем соответствующие подчинённые матричные нормы:

*   **Норма, подчинённая $\|\cdot\|_1$ (столбцовая норма):**
    $$
    \|A\|_1 = \max_{1 \le j \le n} \sum_{i=1}^n |a_{ij}|
    $$
    *(максимальная сумма модулей элементов по **столбцам*)*

*   **Норма, подчинённая $\|\cdot\|_\infty$ (строчная норма):**
    $$
    \|A\|_\infty = \max_{1 \le i \le n} \sum_{j=1}^n |a_{ij}|
    $$
    *(максимальная сумма модулей элементов по **строкам*)*

*   **Норма, подчинённая $\|\cdot\|_2$ (спектральная норма):**
    $$
    \|A\|_2 = \sqrt{\lambda_{\max}(A^T A)}
    $$
    где $\lambda_{\max}$ — наибольшее собственное значение матрицы $A^T A$.

### **Треугольные матрицы**


#### 🔺 Верхняя треугольная матрица

Все элементы **ниже** главной диагонали равны нулю:
$a_{ij} = 0$ при $i > j$.

**Пример:**

$$
U = \begin{pmatrix}
2 & -1 & 3 \\
0 & 4 & 5 \\
0 & 0 & 6
\end{pmatrix}
$$

Здесь $u_{ij} = 0$ при $i > j$.

---

#### 🔻 Нижняя треугольная матрица

Все элементы **выше** главной диагонали равны нулю:
$a_{ij} = 0$ при $i < j$.

**Пример:**

$$
L = \begin{pmatrix}
1 & 0 & 0 \\
7 & -2 & 0 \\
3 & 8 & 5
\end{pmatrix}
$$

Здесь $l_{ij} = 0$ при $i < j$.


---

### **3. Сходимость по норме**

Последовательность векторов $\{x_n\}$ является сходящейся по норме к вектору $x$, если норма разности стремится к нулю при $n \rightarrow \infty$.

**Математически:**

$$
\lim_{n \rightarrow \infty} \|x_n - x\| = 0
$$


